BOT_TOKEN=your_bot_token_here
ADMIN_IDS=[749256529]
DATABASE_URL=postgresql+asyncpg://user:password@host:5432/dbname
LOG_LEVEL=INFO

# Legacy proxy for disabled Avito diagnostics only.
# Формат: http://user:pass@host:port  или  socks5://user:pass@host:port
# Auto.ru never reads this value.
PROXY_URL=http://username:password@proxy-host:port
# Dedicated proxy used only by Auto.ru.
AUTORU_PROXY_URL=
# PROXY_URL is ignored by Auto.ru.
AUTORU_TIMEOUT_SECONDS=15

# Rest-App Avito shared collection/cache settings.
REST_APP_MAX_TIME_WINDOWS=6
REST_APP_CACHE_TTL=300
REST_APP_RESULT_LIMIT=50
REST_APP_DB_MAX_AGE_HOURS=24
REST_APP_DEMO_RELAX_PRICE=true

# Avito source strategy:
# rest_app  - primary: REST-App API (fast, lots of data)
# playwright - primary: browser scraping (fallback / reserve)
AVITO_SOURCE=rest_app

# REST-App API credentials: NEVER write real tokens here or in logs.
REST_APP_LOGIN=
REST_APP_TOKEN=
REST_APP_ENABLED=true
REST_APP_REQUEST_INTERVAL=60
REST_REQUEST_INTERVAL=60
REST_APP_LAST_MINUTES=3
REST_APP_CACHE_TTL_SECONDS=55
REST_APP_MAX_PAGES=1
REST_APP_DAILY_SOFT_LIMIT=8000
REST_APP_DAILY_HARD_LIMIT=9500
REST_APP_CACHE_TTL=300
REST_APP_MAX_TIME_WINDOWS=6
REST_APP_RESULT_LIMIT=50
REST_APP_DB_MAX_AGE_HOURS=24
REST_APP_DEMO_RELAX_PRICE=true
REST_APP_TIMEOUT=30

# Avito history tracking (new / price changes / relisted)
AVITO_HISTORY_ENABLED=true

# Avito provider (legacy selector, kept for compatibility).
# disabled | rest_app | adspower_worker | playwright
AVITO_ENABLED=true
AVITO_PROVIDER=rest_app
AVITO_HEADLESS=true
AVITO_BROWSER_TIMEOUT_SECONDS=30
AVITO_NAVIGATION_TIMEOUT_SECONDS=30
AVITO_MAX_CONCURRENT_PAGES=1
AVITO_BROWSER_RESTART_AFTER_SEARCHES=50

# Avito proxy: residential pool (used only when AVITO_PROVIDER=playwright).
AVITO_PROXY_HOST=pool.proxys.io
AVITO_PROXY_PORT_START=10000
AVITO_PROXY_PORT_END=10999
AVITO_PROXY_USERNAME=
AVITO_PROXY_PASSWORD=
AVITO_PROXY_PROTOCOL=http
AVITO_PROXY_CHECK_LIMIT=10
AVITO_PROXY_CONNECT_TIMEOUT_SECONDS=8
AVITO_PROXY_STICKY_MINUTES=60

# Avito proxy: legacy single URL (backward compatibility).
AVITO_PROXY_URL=

# Legacy Rest-App / AdsPower settings (used only when AVITO_PROVIDER is rest_app/adspower_worker)
REST_APP_LOGIN=
REST_APP_TOKEN=
REST_APP_TIMEOUT=30
REST_APP_TEST_REGION=Новосибирская область
REST_APP_TEST_CITY=Новосибирск
AVITO_MIN_INTERVAL_SECONDS=120
AVITO_BLOCK_COOLDOWN_SECONDS=7200
AVITO_MAX_INTERNAL_REDIRECTS=1
AVITO_CACHE_TTL_SECONDS=1800
AVITO_STALE_CACHE_TTL_SECONDS=86400
AVITO_MANUAL_WAIT_SECONDS=20

# AdsPower parser-worker. Configure these only on the machine running AdsPower.
ADSPOWER_API_URL=http://127.0.0.1:50325
ADSPOWER_PROFILE_ID=
ADSPOWER_API_KEY=
AVITO_REQUIRE_AUTH=true
AVITO_STOP_PROFILE_ON_EXIT=false
AVITO_MAX_PAGES=2
AVITO_MAX_ITEMS=50
AVITO_DETAIL_MODE=full
AVITO_DETAIL_CONCURRENCY=1
AVITO_PROFILE_START_TIMEOUT=30
AVITO_CDP_CONNECT_TIMEOUT=15
AVITO_NAVIGATION_TIMEOUT=25
AVITO_LOCATOR_TIMEOUT=15
AVITO_LISTING_TIMEOUT=20
AVITO_SEARCH_DEADLINE=180
AVITO_RETRY_ATTEMPTS=2
AVITO_DELAY_MIN_SECONDS=1.2
AVITO_DELAY_MAX_SECONDS=3.5
AVITO_WORKER_HOST=127.0.0.1
AVITO_WORKER_PORT=8081
AVITO_WORKER_TOKEN=

# Railway client settings. Enable only after a successful local diagnostic.
AVITO_WORKER_URL=
AVITO_WORKER_TIMEOUT_SECONDS=190

# Отдельный обязательный proxy только для Авито (Playwright).
# Формат: http://user:pass@host:port  или  socks5://user:pass@host:port
AVITO_PROXY_URL=

DASHBOARD_KEY=change_me_secret
PORT=8080

# ЮMoney: номер кошелька и секрет из Настройки -> HTTP-уведомления.
# URL уведомлений в ЮMoney: https://ВАШ-ДОМЕН/yoomoney/webhook
YOOMONEY_WALLET=
YOOMONEY_NOTIFICATION_SECRET=
PUBLIC_URL=

# Xray/VLESS proxy (credentials via env, NOT in repo):
XRAY_UUID=
XRAY_ADDRESS=ru.axolab.org
XRAY_PORT=443
XRAY_PUBLIC_KEY=
XRAY_SNI=sun9-8.userapi.com
XRAY_SHORT_ID=
XRAY_AUTOSTART=0

# Отключение проверки TLS-сертификатов (только для отладки SOCKS-прокси): DISABLE_SSL_VERIFY=1
DISABLE_SSL_VERIFY=0
