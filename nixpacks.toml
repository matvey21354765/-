[phases.setup]
nixPkgs = ["python311", "chromium"]

[phases.install]
cmds = [
  "python3 -m venv /app/venv",
  "/app/venv/bin/pip install -r requirements.txt",
  "/app/venv/bin/python -m playwright install chromium"
]

[start]
cmd = "/app/venv/bin/python control_bot.py"
