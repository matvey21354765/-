[build]
builder = "NIXPACKS"
buildCommand = "pip3 install -r requirements.txt && python3 -m playwright install chromium"

[deploy]
startCommand = "python control_bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
